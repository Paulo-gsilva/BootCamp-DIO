let lines = gets().split("\n");
let n = parseInt(lines.shift());


if (n >= 1 && n <= 34) {

const text = "Life is not a problem to be solved, but a reality to be experienced.";

const letter = text.substr(0, n)

const uperCase = letter.toUpperCase();

print(uperCase);

}